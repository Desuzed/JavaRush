/*
package lvl6;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class BubSortObjects {
    long array [];
    private int elems;

    public BubSortObjects (int max){
        array=new long[max];
    }
    public void into(long value) {
        array[elems] = value;
    }

        public static void main(String[] args) throws IOException {
        BufferedReader reader=new BufferedReader(new InputStreamReader(System.in));

        BubbleSorter mas=new BubbleSorter();

            long n=Integer.parseInt(reader.readLine());
            BubSortObjects.into(n);
            System.out.println("Проинициализируйте массив");
            for (int i = 0; i < n; i++) {
                mas[i]=Integer.parseInt(reader.readLine());
            }
    }
}

*/
